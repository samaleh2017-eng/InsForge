{
  "buildCommand": "npm ci --workspaces && npm run build:vercel",
  "outputDirectory": "dist/frontend",
  "rewrites": [
    {
      "source": "/:path*",
      "destination": "/index.html"
    }
  ]
}
